let obj = {
    id: 100,
    name: "Hong",
    birth: "1999-12-25"
}

let {id, name, birth, age} = obj;

console.log(id, name, birth, age);


let array = ["Hello", "Nice", "Friday"];

let [idx1, idx2, idx3] = array;

console.log(idx1, idx2, idx3);